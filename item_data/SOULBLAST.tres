[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://scripts/item_data.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
item_id = 32
item_name = "soul blast"
item_desc = "hold ↓  then attack to do a slash with 4 hit priority then throw a slightly ascending projectile with 5 hit priority"
