[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://scripts/item_data.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
item_id = 512
item_name = "silicone pad"
item_desc = "\"streamer, why were you so paranoid of the box this was in?\"

do a DR input then attack to throw a bouncy attack with 20 hit priority
"
